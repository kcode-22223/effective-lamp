name: Stream Real Samra GUI to Browser
on: [workflow_dispatch]

jobs:
  launch-samra-desktop:
    runs-on: macos-14
    steps:
      - name: Checkout Repository Files
        uses: actions/checkout@v4

      - name: Download and Install Samra v1.4.1
        run: |
          curl -L -o Samra.zip https://github.com/NSAntoine/Samra/releases/download/1.4.1/Samra.app.zip
          unzip Samra.zip
          sudo mv Samra.app /Applications/

      - name: Configure Built-in macOS VNC Server
        run: |
          # Sets up a temporary security credential for the session connection
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure -access -on \
            -clientopts -setvncpasswd -vncpasswd password123 \
            -restart -agent -privs -all

      - name: Launch Web Proxy Display Tunnel
        run: |
          echo "=========================================================="
          echo "👉 CLICK THE SECURE URL BELOW TO CONTROL THE CLOUD MAC:"
          echo "=========================================================="
          
          # Creates a secure reverse-proxy path to port 5900 (macOS VNC Stream)
          curl -s https://pinggy.io | sh -s -- -p 5900
