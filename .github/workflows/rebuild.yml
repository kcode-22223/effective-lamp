name: Automate Samra Asset Replacement
on: [workflow_dispatch]

jobs:
  replace-asset:
    runs-on: macos-14
    steps:
      - name: Checkout Repository Files
        uses: actions/checkout@v4

      - name: Download and Install Samra App
        run: |
          curl -L -o Samra.zip https://github.com/NSAntoine/Samra/releases/download/1.4.1/Samra.app.zip
          unzip Samra.zip
          sudo mv Samra.app /Applications/

      - name: Execute Robust AppleScript GUI Automation
        run: |
          killall Samra || true
          open /Applications/Samra.app
          sleep 4

          osascript -e '
            tell application "System Events"
              tell process "Samra"
                set frontmost to true
                delay 1
                
                # 1. Bypass the Welcome Screen by clicking "Open Assets File"
                tell window 1
                  click static text "Open Assets File"
                end tell
                delay 1.5
                
                # 2. Input the absolute path to your Assets.car file
                keystroke "G" using {command down, shift down}
                delay 1
                keystroke "'"$PWD"'/Assets.car"
                delay 0.5
                key code 36 -- (Press Enter to apply location)
                delay 1
                key code 36 -- (Press Enter again to confirm file pick)
                
                # CRITICAL: Allow time for a large catalog to fully generate its UI
                delay 8 

                # 3. Target the main application workspace window now that it is open
                tell window 1
                  # Target the search bar and filter for "watermark"
                  keystroke "f" using command down
                  delay 1
                  keystroke "watermark"
                  delay 2 -- Let search indexing filter the row elements

                  # Highlight and select the structural element row
                  set targetRow to static text "watermark"
                  select targetRow
                  delay 1.5

                  # Click the visual "Edit" button in the details panel
                  click button "Edit"
                  delay 2
                end tell

                # 4. Handle the file picker sheet for the replacement image
                keystroke "G" using {command down, shift down}
                delay 1.5
                keystroke "'"$PWD"'/replacement_watermark.png"
                delay 1
                key code 36 -- (Confirm path input)
                delay 1.5
                key code 36 -- (Confirm target selection)
                delay 3

                # 5. Save the corrected data modifications
                keystroke "s" using command down
                delay 2
              end tell
            end tell
          '

      - name: Export Finished File Artifact
        uses: actions/upload-artifact@v4
        with:
          name: rebuilt-asset-catalog
          path: Assets.car
